
//Node module, assert can be imported at the top of your files
const assert = require('assert');


describe ('getInterSection', () => {
    it('returns nest variables, while loop and if statement')

    describe('Math', () => {
        describe('.min', () => {
          it('returns the argument with the lowest value', () => {
      
          })
        })
      })
    
      describe('Math', () => {
        describe('.max', () => {
          it('returns the argument with the high value', () => {
      
          })
        })
      })
})

describe('one++', () => {
    it('returns the sum of its arguments', () => {
      assert.ok
    });
  });

  describe('two++', () => {
    it('returns the sum of its arguments', () => {
      assert.ok
    });
  });


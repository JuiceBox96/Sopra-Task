const assert = require('assert');

describe('Math', () => {
    describe('.min', () => {
      it('returns the argument with the lowest value', () => {
  
      })
    })
  })


  describe('Math', () => {
    describe('.max', () => {
      it('returns the argument with the lowest value', () => {
  
      })
    })
  })

//Node module, assert can be imported at the top of your files
const assert = require('assert');

// test for Math. functions
describe ('getInterSection', () => {


    describe('Math', () => {
        describe('.min', () => {
          it('returns the array with the lowest value', () => {
      
          })
        })
      })

      describe('Math', () => {
        describe('.max', () => {
          it('returns the array with the highest value', () => {
          })
        })
      })
    })

// test for if statement increments 
describe('one++', () => {
    it('returns the sum of its arguments', () => {
    });
  });

  describe('two++', () => {
    it('returns the sum of its arguments', () => {
    });
  });

